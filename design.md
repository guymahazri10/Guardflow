# GuardFlow — מערכת עיצוב

זהות ויזואלית אחת לכל האפליקציה: כחול ביטחוני כאקצנט ראשי, זהב כסימן דרגה (מנהל בלבד), וסקאלת סטטוסים מלאה (הצלחה/אזהרה/שגיאה/מידע) נפרדת מהמותג. כותרות משלבות משקל קליל ומשקל שחור באותו משפט, כפתורים ועיגולים הם האלמנט האינטראקטיבי החוזר. המסמך הזה הוא מקור האמת לטוקנים, לקומפוננטות ולכללים — לצד `design_system.html` שמדגים אותם על מסכי האפליקציה האמיתיים (הערה: הארטיפקט טרם עודכן לפלטת הכחול — התוכן החי בקוד הוא המקור המדויק ביותר כרגע).

> **הערת גרסה:** זו הגרסה השלישית של מערכת העיצוב. הראשונה ("תג" — נייבי+פליז) והשנייה ("רך ובוטח" — מונוכרום שחור על קרם) הוחלפו לפי בקשה מפורשת לפלטת מותג קבועה: כחול ביטחוני + זהב + סטטוסים. **הצורה, הטיפוגרפיה, הרדיוסים והתנועה מהגרסה הקודמת נשארו** — רק הצבעים השתנו.

## עקרון-על: משטח אחד, אקצנט אחד, זהב לדרגה בלבד

כל מסך יושב על אותו רקע ניטרלי בהיר (לא גרדיאנט קרם יותר — רקע שטוח). **כחול (`primary`) הוא האקצנט הפעיל היחיד** — כפתורים, מצב פעיל, טבעות פוקוס, אווטארים. **זהב מוגבל לדרגה** — כרגע רק לתג התפקיד "מנהל", לא לכפתורים או פעולות רגילות. סטטוסים (הצלחה/אזהרה/שגיאה) הם פלטה שלישית, נפרדת לגמרי מהאקצנט — אף פעם לא מתערבבים איתו.

### האלמנט המזהה (Signature)

**העיגול הכחול המלא.** מצב פעיל בניווט התחתון, אווטארים, מתג אקורדיון (+/−), עיגולי מספור שלבים — כולם עיגול מלא בצבע `primary`, תוכן לבן. **חריג יחיד:** תג התפקיד "מנהל" הוא זהב מלא (`gold`) עם טקסט כהה — סימן דרגה, לא כפתור.

**כותרת דו-משקלית.** כל כותרת מסך בנויה משתי מילים לפחות: הראשונה במשקל קליל (300) בגוון אפור, השנייה במשקל שחור (800) בשחור-כמעט-מלא (`text-primary`, לא כחול — הכחול נשמר לאלמנטים אינטראקטיביים, לא לטקסט רץ). לדוגמה: <span style="font-weight:300">משמרת</span> <b>אחה״צ</b>, <span style="font-weight:300">ניהול</span> <b>משתמשים</b>.

---

## טוקנים

### צבע — משטח וניטרלי

| טוקן | ערך | שימוש |
|---|---|---|
| `background` (BG_PAGE) | `#F7F8FA` | רקע כללי, שטוח — לא גרדיאנט |
| `background-2` (BG_SOFT) | `#F1F3F5` | פאנל משני, מצב לא-פעיל |
| `surface` (SURFACE) | `#FFFFFF` | כרטיסים, שדות קלט, גיליון תחתון |
| `border` (BORDER_LIGHT) | `#D9DEE5` | קווי הפרדה, מסגרת כרטיס רגילה |
| `border-strong` (BORDER_STRONG) | `#C8D0D9` | מסגרת מודגשת (למשל שדה בפוקוס-שגיאה) |
| `text-primary` (TEXT_PRIMARY) | `#15171A` | טקסט ראשי |
| `text-secondary` (TEXT_SECONDARY) | `#6F7782` | טקסט משני, תוויות |
| `text-muted` (TEXT_MUTED) | `#98A1AB` | טקסט חלש ביותר, placeholder |

### צבע — מותג (כחול ביטחוני)

| טוקן | ערך | שימוש |
|---|---|---|
| `primary` (PRIMARY_500) | `#1B56A5` | כפתור ראשי, מצב פעיל, אווטאר, טבעת פוקוס |
| `primary-600` (PRIMARY_600) | `#123F7A` | hover/מעבר עכבר (דסקטופ) |
| `primary-dark` (PRIMARY_700) | `#0B2D59` | מצב לחיצה (`active:`) — כהה יותר מה-hover |
| `primary-light` (PRIMARY_100) | `#E8EFF8` | רקע פעולה בהיר — צ׳יפ אחמ"ש, `btn-ghost` בלחיצה |
| `primary-200` (PRIMARY_200) | `#C9D8EC` | טבעת פוקוס על שדות קלט |

**כלל:** כחול הוא **גם** צבע הטקסט הראשי? **לא** — טקסט ראשי הוא `text-primary` (כמעט-שחור), כחול שמור לאלמנטים אינטראקטיביים בלבד. זה שונה מהגרסה הקודמת המונוכרומטית, ומכוון: כחול-על-טקסט-רץ מרגיש כמו קישור שבור.

### צבע — זהב (דרגה בלבד)

| טוקן | ערך | שימוש |
|---|---|---|
| `gold` (ACCENT_GOLD) | `#D4A24A` | תג תפקיד "מנהל" בלבד — מילוי מלא, טקסט `text-primary` כהה עליו (לא לבן — ניגודיות) |
| `gold-soft` (ACCENT_GOLD_SOFT) | `#FFF1D8` | שמור לשימוש עתידי (למשל רקע להדגשת "דרגה" בהקשר אחר) — לא בשימוש היום מעבר לתג |

**כלל:** זהב **לא** מופיע בכפתורים, בניווט, או בכל פעולה רגילה — רק כסימן דרגה על "מנהל". אם יתווסף שימוש נוסף, הוא צריך להישאר באותה משמעות ("הכי גבוה/מיוחד"), לא כאקצנט כללי שני.

### צבע — סטטוסים (נפרד לגמרי מהמותג)

| טוקן | ערך | רקע (`-light`) | שימוש |
|---|---|---|---|
| `good` (SUCCESS) | `#2F7D4A` | `#E5F2E9` | "פורסם", טוסט הצלחה |
| `warning` (WARNING) | `#A66B13` | `#F8EDD8` | "טיוטה"/ממתין |
| `danger` (ERROR) | `#B63A32` | `#FBE8E6` | מחיקה, יציאה, שגיאה |
| `info` (INFO) | `#17627A` | `#DDF3F7` | הודעות מידע (טרם בשימוש פעיל — מוגדר ומוכן) |

מרוסנים ולא זהים לכחול המותג — כדי שסטטוס "פורסם" (ירוק) לעולם לא יתבלבל עם פעולה רגילה (כחול).

### צבע — עמדות (Position palette)

פלטה נפרדת ומכוונת — לא קשורה לצבעי המותג/סטטוס, ולא משתנה יחד איתם. תפקידה לאפשר זיהוי עמדה במבט חטוף גם כשהעמדה ריקה:

| עמדה | טוקן | ערך |
|---|---|---|
| לובי | `position.lobby` | `#c99a4f` |
| סריקה / פרימטר | `position.perimeter` | `#5f9e72` |
| כונן | `position.standby` | `#9868b8` |
| הפסקה | `position.break` | `#a89c8a` |
| שובר שגרה | `position.routine` | `#d6823f` |
| חילוף | `position.relief` | `#3f9aa8` |
| סגירה | `position.close` | `#4f7fc4` |
| עמדה חיצונית / כיכר | `position.exterior` | `#c1613f` |
| ניהול / ביקורות | `position.management` | `#1B56A5` (= primary — הדרגה הניהולית קשורה לכחול המותג בכוונה) |

נגזרים לפילס: **משובץ** = מילוי `color @ 22%`; **פנוי** = קונטור בלבד (`border: color @ 50%`, ללא מילוי) — הגוון נשאר ניכר גם ריק, כדי שאפשר יהיה להבדיל "כונן" מ"סריקה" גם לפני שיבוץ.

### צבע — תפקיד משתמש (Role chips)

| תפקיד | bg | color | הגיון |
|---|---|---|---|
| מנהל | `gold` מלא | `text-primary` (כהה) | סימן דרגה — היחיד שמשתמש בזהב |
| אחמ"ש | `primary-light` | `primary` | תפקיד פונקציונלי — כחול עדין |
| מאבטח | `background-2` | `text-secondary` | ברירת מחדל, הכי שקט |

### טיפוגרפיה, רדיוסים, צללים, תנועה

**ללא שינוי מהגרסה הקודמת** — Heebo בלבד (300–900), כותרת דו-משקלית, `tabular-nums` לספרות, רדיוסים 12/18/24/999px, צללים רכים ומפוזרים (`shadow-card` / `shadow-card-md`), כניסת רשימה מדורגת בטעינה ראשונה בלבד, `prefers-reduced-motion` מכבה אנימציות לא-חיוניות.

---

## קומפוננטות

### כפתורים

- **Primary**: מילוי `primary`, טקסט לבן, פינות מלאות (`rounded-full`). מצב לחיצה: `primary-dark` (לא רק `scale`). `disabled` → `opacity:.4`.
- **Secondary/Ghost**: שקוף, מסגרת `primary/70`, טקסט `primary`, לחיצה → רקע `primary-light`.
- **Danger** (טקסט, כמו "יציאה מהמערכת"): טקסט `danger` בלבד, לחיצה → רקע `danger-light`.
- **Danger מלא** (אישור מחיקה): מילוי `danger`, טקסט לבן.

### שדות קלט

רקע `surface` (לבן), מסגרת `border`, רדיוס 12px, פוקוס: מסגרת `primary` + טבעת 2px `primary-200`.

### תג הקצאה (Badge עמדה)

**הכלל: מלא מול קונטור, לא צבע בלבד** — ראו "עמדות" למעלה.

### שורת שיבוץ (Roster row)

סדר קבוע: **תווית תפקיד → שם שומר → תג עמדה**. בעברית/RTL זה אומר שתווית התפקיד ("מאבטח 1", "אחמ"ש") יושבת מימין (ראשון שנקרא), השם באמצע במשקל מודגש, ותג העמדה הצבעוני משמאל. *(תוקן מגרסה קודמת של המסמך שתיארה סדר הפוך — הסדר הנוכחי אומת מול המשתמש בקוד החי.)*

### עיגול כחול (Circle) — הרכיב המזהה

גודל בסיס 32px, רקע `primary`, תוכן לבן. שימושים: מתג אקורדיון (+/−), מספר שלב, מצב פעיל בניווט (עיגול מאחורי האייקון הפעיל בלבד). **אווטאר** — אותו עיגול, `primary` מלא, אות ראשונה לבנה, משקל 800.

### אקורדיון

כותרת + עיגול כחול (+/−) בקצה. קו הפרדה דק בין פריטים בתוך אותו כרטיס. בשימוש בפועל: "הערות למשמרת" בעורך הלו״ז.

### ניווט תחתון (Signature #2)

פס צף מזוגג לבן-שקוף (`rgba(255,255,255,.85)` + blur). פריט פעיל: עיגול `primary` מלא מאחורי האייקון, טקסט `primary` מודגש. פריטים לא-פעילים: קו `text-secondary` דק, בלי רקע.

### מצבי טעינה / ריק / שגיאה

שלד פועם בטוקני `background-2`/`border`. איקון SVG קווי, **לא אימוג׳י** — הכלל הזה חל גם על אייקוני-קישוט אחרים באפליקציה (הוסרו גם אימוג׳י מזג-האוויר של סוגי המשמרת ברשימות ניהול הלו״זים — 🌅/⛅/🌙 — כי הם לא נשאו מידע, רק קישטו).

### טוסט

כרטיס `surface`, `shadow-card-md`, עיגול קטן בצבע סמנטי (`good`/`danger` בהצלחה/שגיאה בלבד — לא `primary`).

---

## נגישות

- ניגודיות `text-primary`/`text-secondary` על `surface`/`background` ≥ 4.5:1.
- טקסט על `gold`: **כהה** (`text-primary`), לא לבן — לבן-על-זהב לא עומד ב-AA.
- כל אלמנט אינטראקטיבי מקבל `:focus-visible` ברור — טבעת `primary-200`.
- שטח מגע מינימלי 44×44px גם לעיגולים הקטנים (32px ויזואלי + padding שקוף).
- מצב הקצאה (משובץ/פנוי) קריא מהצורה (מלא/קונטור) בנוסף לצבע.
- `prefers-reduced-motion: reduce` מכבה אנימציות לא-חיוניות.

---

## מיפוי ל-Tailwind (כפי שמיושם בפועל ב-`tailwind.config.js`)

```js
colors: {
  primary: '#1B56A5',
  'primary-600': '#123F7A',
  'primary-dark': '#0B2D59',
  'primary-light': '#E8EFF8',
  'primary-200': '#C9D8EC',
  gold: '#D4A24A',
  'gold-soft': '#FFF1D8',
  surface: '#ffffff',
  background: '#F7F8FA',
  'background-2': '#F1F3F5',
  border: '#D9DEE5',
  'border-strong': '#C8D0D9',
  'text-primary': '#15171A',
  'text-secondary': '#6F7782',
  'text-muted': '#98A1AB',
  good: '#2F7D4A', 'good-light': '#E5F2E9',
  warning: '#A66B13', 'warning-light': '#F8EDD8',
  danger: '#B63A32', 'danger-light': '#FBE8E6',
  info: '#17627A', 'info-light': '#DDF3F7',
  position: {
    lobby: '#c99a4f', perimeter: '#5f9e72', standby: '#9868b8',
    break: '#a89c8a', routine: '#d6823f', relief: '#3f9aa8',
    close: '#4f7fc4', exterior: '#c1613f', management: '#1B56A5',
  },
},
borderRadius: { card: '16px', badge: '999px' },
```

גופן: **Heebo בלבד**, ממשקל 300 עד 900. `getPositionBadgeStyle` ב-`src/constants/colors.ts` מקבל `assigned: boolean` (ברירת מחדל `true`) לבחירה בין מילוי לקונטור-צבעוני.

---

## לוגו

`src/assets/logo.png` — הקובץ המקורי (רקע לבן, שחור). `src/assets/logo_full.png` (סמל+כיתוב, רקע שקוף, מסך התחברות) ו-`logo_icon.png` (סמל בלבד, מסך קביעת סיסמה) — הלוגו עצמו נשאר שחור, לא נצבע מחדש בכחול; הוא נכס קבוע. אם ירצו גרסה כחולה בעתיד — צריך קובץ מקור וקטורי (SVG), לא רק לשנות CSS על ה-PNG.

## מה נשאר כמו שהיה, בכוונה

- מבנה הגיליונות התחתונים (bottom sheets).
- הפרדת `dir="ltr"` לשדות אימייל/סיסמה/שעות מול `dir="rtl"` לשאר הממשק.
- הנוסחה לגזירת פילס-בהיר מצבע בסיס לעמדות (22% למשובץ, קונטור 50% לפנוי).
- הטון של הטקסטים בעברית.
- כל הטיפוגרפיה, הרדיוסים, הצללים והתנועה מגרסת "רך ובוטח" — רק הצבעים השתנו בבקשה מפורשת.
