export function ShiftSetupPage() {
  return <PagePlaceholder title="הגדרת משמרת" />
}

function PagePlaceholder({ title }: { title: string }) {
  return <h2 className="text-xl font-semibold">{title} (בקרוב)</h2>
}
